# Final validation report

Status: **the first seventeen product-convergence C0 request/authority slices and the preserved M0-M12 foundation are locally validated through August 4, 2026**. C0 as a whole remains in progress because 11 of 127 enumerated tenant-sensitive entry points are not yet bound and no selected managed PostgreSQL deployment has been exercised. This is not a managed-deployment, production-restore/PITR, security-certification, live-provider, vendor-semantic, external-data-right, cross-customer-rights, customer-baseline, broker-savings, programme-effectiveness, caused-loss-reduction, source-rights, legal-currentness, model-validity, semantic-acceptance, counterparty-authority, real-credential, physical-inspection, resilience-effectiveness, external-verifier, insurance-outcome, real-funding/payment, recipient-delivery, carrier-acceptance, customer-validation, programme-validation, or market-validation claim.

## Product convergence C0 request/authority local-validation addendum

The second C0 slice adds an executable inventory of 127 tenant-sensitive entry points and binds eight: the four-route brokerage family, authenticated session read and logout, inbound integration webhook, and document worker. The third slice binds the three-route access-control family. The fourth slice binds the five-route storage family. The fifth slice binds membership invitation creation and reason-bound revocation. The sixth slice binds local development sign-in and OIDC start/callback. Global attempt and rate-limit work runs under `fortify_app`; tenant-owned invitation, acceptance, membership, and session work runs under transaction-local RLS, while OIDC network exchange remains outside database transactions. Exact invitation, state, and provider-verified-identity lookup returns only an organization identifier. The seventh slice binds community read/update and property-graph workspace/registration. The eighth binds document workspace/intake/review/retry. The ninth binds all seven portfolio-import handlers. The tenth binds all five governed-source handlers. The eleventh binds all five market-playbook handlers. The twelfth binds all seven resilience-planning handlers. The thirteenth binds all fifteen funding/project-execution handlers. The fourteenth binds all sixteen independent-verification handlers. The fifteenth binds all nine authenticated integration handlers, completing that ten-route family with its previously bound signed inbound webhook and taking coverage to 93/127. The sixteenth binds all twelve authenticated model-recognition handlers, taking coverage to 105/127. Presenters expose only UI-consumed onboarding, property/document, import-review, source/version/review/publication/dependency/alert, destination/requirement/rule/case/link, planning profile/criteria/applicability/intervention/gap/scenario, funding programme/rule/application/stack/commitment/milestone/export/collaborator/benefit, verification organization/credential/assignment/method/finding/exception/certificate/maintenance history, integration connection/schema/job/attempt/receipt/webhook/health/provider data, model provider/version/definition/output/mapping/evidence/event/organization/commitment data, project-scoped access, human-history, doctrine, and bounded receipt fields. They omit raw import rows, tenant/storage custody, candidate/applied entity identifiers, idempotency/request hashes, receipt summaries, provider/worker diagnostics and errors, raw integration configuration/payloads, raw model-output values and unused mapping reviews, persistence metadata, internal actors, opaque-token and credential identifiers/digests, raw disbursement payloads, eligibility input facts/hashes, unused source/destination/project/link fields, confirmation flags, and unused lifecycle/revision fields. Actual route attacks cover tenant-only reads, cross-tenant property/document/portfolio/source/playbook/planning/funding/project/verifier/credential/assignment/integration/model-recognition resource denial, read-only mutation denial, unchanged foreign-tenant state, minimized successful responses, inline-secret rejection, idempotent integration queueing, fail-closed unavailable providers, append-only dead-letter replay, three-person model and commitment publication, independent mapping review, human-confirmed mapping events, three-person source/profile/programme publication authority, independent playbook/intervention/commitment/credential/finding/certificate decisions, separate payment approval/export, and scoped external-project/verifier access and revocation. Request proofs exposed and repaired both project- and verification-token digest mismatches that had prevented valid collaborator/verifier credentials from resolving through bootstrap. Every bound authenticated route awaits the wrapper inside its local error boundary; the inventory fails on the unsafe unawaited form.

The seventeenth slice binds all eleven authenticated recognition handlers, taking coverage to 116/127 and leaving only programme analytics unbound. Organization and external-reviewer presenters expose only UI-consumed exact submission, delivery, receipt, scoped session, correspondence, separated outcome, closure, maintenance, artifact, evidence, mapping, and doctrine fields. They omit tenant/write/lifecycle metadata, opaque token digests, external-principal/grant identifiers, internal actors, and storage custody. Actual route attacks cover tenant-only workspaces, read-only mutation denial, cross-tenant submission/delivery/closure denial, exact reviewer-principal isolation and revocation, explicit human confirmation, separated evidence/model/rating/underwriting/placement/funding outcomes, and minimized responses.

Migration 0028 adds a fixed-search-path `SECURITY DEFINER` bootstrap executable only by `fortify_app`; it returns only an organization identifier for exact opaque credential material or an active endpoint key. Full identity, membership, role, scope, assignment, expiry, revocation, HMAC, and resource authorization are revalidated after transaction-local RLS context is set. The bootstrap contract test verifies definer mode, fixed search path, `fortify_app` execution, and no `PUBLIC` execution; session and API-credential request tests exercise RLS without repository tenant predicates, and webhook tests exercise active/unknown endpoint resolution.

The sixteenth slice's complete local release evidence passes scoped Prettier, full ESLint, strict TypeScript, 34 isolated Vitest files/125 tests, 33/33 production-flow mapping, the 13-artifact operations contract, executable 127-entry inventory with 105 bound and 22 unbound, a 459-file secret scan, production build, 12/12 deterministic sandbox evaluation, the 18-pattern claims scan, 56 Playwright passes with four intentional tablet/mobile skips, and `npm audit --audit-level=moderate` with zero vulnerabilities. Schema generation remains 190 tables with no drift. Fresh portfolio-onboarding inspection at 1280×720, 834×1112, and 390×844 found no document-width overflow or browser console errors. Hydrated controls previewed two accepted, one ambiguous, and one rejected row; committed only the human-confirmed accepted rows; then rolled them back while retaining all rows and preview/commit/rollback receipts. Form/action controls measured 38-39 pixels, mobile row filters 28 pixels, and the dismiss control 20 pixels, below the future 44-pixel C2 design-system target; the milestone/fixture presentation and internally scrolling 613-pixel table inside its 328-pixel mobile container remain visible. The governed-source and market-playbook workflows separately passed automated desktop/tablet/mobile journeys and hydrated 1280×720 inspections with the recorded authority and fail-closed boundaries. The resilience-planning workflow passed the same viewport matrix; a fresh hydrated 1280×720 inspection exercised all four planning views and the insufficient-evidence stop state without document-width overflow or console errors. The funding/project-execution workflow passed the same automated viewport matrix; a fresh hydrated 1280×720 inspection exercised all four views, the insufficient-evidence stop, exact export boundary, and preview revocation without document-width overflow or console errors. The verification workflow passed the same automated viewport matrix and all six governed scenarios; a fresh hydrated 1280×720 inspection traversed all four views with no document-width overflow or application console errors. The integration workflow passed the same automated viewport matrix; a fresh hydrated 1280×720 inspection exercised dead-letter and disconnected states plus all four views with no document-width overflow or application console errors. Integration tabs measured 48 pixels; the 32.5-pixel native scenario select with 9-pixel text and the milestone/fixture presentation remain C2 debt. The model-recognition workflow passed the same automated viewport matrix; a fresh hydrated 1280×720 inspection exercised all three views plus unsupported and no-commitment states without document-width overflow or application console errors. Model-recognition tabs measured 48 pixels; the 30.5-pixel native scenario select with 9-pixel text and the milestone/fixture presentation remain C2 debt. C2 therefore remains incomplete.

The seventeenth slice's complete local release evidence passes scoped Prettier, full ESLint, strict TypeScript, 34 isolated Vitest files/128 tests, 33/33 production-flow mapping, the 13-artifact operations contract, executable 127-entry inventory with 116 bound and 11 unbound, a 459-file secret scan, production build, 12/12 deterministic sandbox evaluation, the 18-pattern claims scan, 56 Playwright passes with four intentional tablet/mobile skips, and `npm audit --audit-level=moderate` with zero vulnerabilities. Schema generation remains 190 tables with no drift. A fresh hydrated 1280×720 recognition inspection exercised submission, secure-reviewer, separated-decision, and maintenance views plus blocked and revoked states with no document-width overflow or application console errors. Tabs measured 48 pixels and the native select 32.5 pixels; milestone/fixture presentation remains C2 debt. C2 therefore remains incomplete.

Migration 0027 creates the non-login `fortify_app` role with `NOSUPERUSER`, `NOCREATEDB`, `NOCREATEROLE`, `NOINHERIT`, and `NOBYPASSRLS`; revokes public table/sequence access; grants only schema/table/sequence runtime privileges; establishes forward default privileges; and adds a unique organization/previous-hash audit-chain index. Runtime now requires a separate `FORTIFY_APP_DATABASE_URL`; equality with the privileged migration/backup `DATABASE_URL` fails closed. The release workflow requires both secrets.

`withTenantTransaction` drops each checked-out transaction to `fortify_app` and binds `fortify.organization_id` and `fortify.actor_subject` through transaction-local settings. The new PGlite PostgreSQL contract exercises the real role and all generated RLS policies without repository organization predicates: organization A sees only A, a write carrying organization B is rejected, organization B subsequently sees only B, and a role-bound transaction without tenant context sees no tenant rows or leaked setting. This is strong local PostgreSQL-compatible evidence, but not selected managed-provider validation and not proof that every current entry point uses the primitive.

Audit append now obtains a per-organization transaction advisory lock and selects the true unreferenced chain tail before hashing. The first full-suite run correctly failed because same-timestamp events exposed the former timestamp/UUID tail-selection defect; after the tail query repair, the affected access-control suite passed 5/5 and the full bounded suite passed. The unique index prevents two successors from referencing the same prior hash.

OIDC attempts, membership invitations, and one-use storage download grants now use conditional update-and-return claims inside their transaction. Concurrent local tests require exactly one fulfilled consumer and one rejected consumer. A failed downstream presign or audit append rolls the storage claim back.

Final local evidence after repairs:

- scoped Prettier, ESLint, and strict TypeScript passed;
- bounded Vitest passed 24 isolated files and 94 tests;
- normalized production-flow mapping passed 33/33 and the operational contract found 13 required artifacts plus RLS/CSP/CSRF/readiness controls;
- the tracked-file secret scan passed 440 files;
- the production build passed with the preserved 30 product pages and 134 API routes;
- deterministic sandbox evaluation passed 12/12 with the unchanged 11,602-byte PDF, 51,723-byte ZIP, and manifest hash `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`;
- the prohibited-claims scan passed all 18 direct patterns;
- Playwright passed 56 scenarios with four intentional tablet/mobile skips for Chromium-owned Axe/visual baselines; and
- live `npm audit --audit-level=moderate` initially found the newly disclosed pinned PostCSS issue, PostCSS was updated to 8.5.25, and the final audit reported zero vulnerabilities.

Fresh desktop, tablet, and mobile brokerage/access captures were inspected. There was no observed clipping, collision, or document-width overflow. They also confirm that milestone-labelled workspaces and synthetic fixture navigation are still visible, so the C2 unified case-first shell and design-system conversion remain explicitly incomplete.

## Replacement north-star M12 operational hardening local-validation addendum

The final consolidated `npm run verify` passed scoped Prettier, ESLint, strict TypeScript, all 24 isolated Vitest files/93 tests, 33/33 mapped normalized-PostgreSQL fixture steps, the 13-artifact operational contract gate, the 432-file secret scan, the 30-page/134-API production build plus Next 16 proxy, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 56 Playwright passes with four intentional tablet/mobile skips for Chromium-owned Axe/visual baselines. `npm audit --audit-level=moderate` reported 0 vulnerabilities; `git diff --check` passed. A prior in-sandbox consolidated attempt reached the browser phase but could not bind `127.0.0.1:3000` (`listen EPERM`); the complete command was rerun outside that socket restriction and passed end to end.

Migration 0026 adds a global ephemeral request-rate window and dynamically enables `fortify_tenant_isolation` RLS on every table with `organization_id`. Fresh migration readback measured 190 tables, 637 unique guards/triggers, and 185 RLS policies. The rate-limit test proved bounded requests and HMAC-only stored buckets; environment tests proved fail-closed secret/HTTPS validation; redaction tests removed nested email and authorization values; authenticated AES-256-GCM backup tests restored exact bytes and rejected a modified authentication tag.

The proxy header/readiness browser contract, Axe scan of portfolio/integrations/programme intelligence, horizontal-overflow checks, and portfolio screenshot baseline all passed. Axe initially found genuine contrast failures; the affected text/avatar colors were darkened and the final scan reported no serious or critical violations. The complete browser run passed every preserved desktop/tablet/mobile journey.

The deterministic six-page Letter PDF and 17-entry ZIP were regenerated. ZIP CRC reported no errors. PDF metadata remained 11,602 bytes, six pages, no JavaScript, and no encryption; cover, evidence index, and caveat/letter pages were visually inspected without observed clipping, collision, broken glyph, or misleading claim. The portfolio visual baseline was inspected at full resolution.

Live M11 CI logs were used as falsification evidence: the quality job exhausted PGlite worker processes and the container build lacked Python for native `better-sqlite3`. M12 runs each test file in a fresh bounded-memory process and installs Python/make/g++ only in the dependency build stage. Docker is not installed on the local host, so the repaired container build and Trivy result remain pending on the M12 PR. No staging/production environment, non-owner managed RLS role, OIDC/MFA, private KMS/storage/scanner, alert route, PITR schedule, managed restore exercise, load result, penetration test, or owner-controlled GitHub setting was validated. Launch status remains deployment-blocked and all customer/programme/verifier/model/market gates remain external.

## Replacement north-star M11 production integrations local-validation addendum

The M11 release surface passed one consolidated `npm run verify`: ESLint, strict TypeScript, 23 Vitest files/89 tests, a 403-file secret scan, the 30-page/133-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 51/51 serial Playwright scenarios across desktop Chromium, an 834-pixel tablet, and Pixel 7. Production schema generation reported 189 tables and no drift; the migrated database exposes exactly 637 unique guards/triggers. The deny-by-default registry covers all 185 resource classes. `npm audit --audit-level=moderate` separately reported 0 vulnerabilities, and `git diff --check` passed.

The M11 migrations add nine tenant-owned tables for version-pinned connections and append-only state history; immutable field-mapping schemas; durable sync jobs and immutable attempts; exact stored receipts; HMAC webhook endpoints and exact-body deliveries; and append-only provider health. Database guards independently enforce tenant equality, current provider/schema lineage, direction/resource/page bounds, exact dead-letter successor replay, attempt/job consistency, successful-attempt and clean-storage receipt custody, webhook credential/connection/quarantine/job alignment, globally unique public endpoint keys, restricted current-state transitions, and immutable history.

The provider catalog names Microsoft Graph email, Gmail email, Google Drive, generic AMS, Applied Epic-compatible, AMS360-compatible, property-management, external-model, and independent-verifier boundaries. Connection configuration rejects inline secret/token/password/API-key/credential fields recursively. Provider type, key, version, and fixture/live mode must match the registered adapter. Live credentials are resolved at execution time through scoped API credential references and are not persisted in connection configuration or sync receipts.

Microsoft Graph is the reference deterministic intake contract. Its fixture proves bounded page sizes, stable cursors, source references, retryable rate-limit failures with retry-after, idempotent queueing, and health history. One durable execution processes one pull page or push batch. Attempts preserve counts, cursors, rate limits, errors, and provider pins. Exhaustion produces a dead letter; a human-confirmed replay creates an immediate successor job with the same request hash while retaining every failed attempt. Successful receipt JSON is written behind the storage adapter and registered only after independent exact-byte size and SHA-256 readback.

Public webhook intake uses a globally unique opaque endpoint key plus a scoped HMAC-SHA-256 credential, event allowlist, and timestamp replay window. Verification precedes storage. A valid body must also decode as a JSON object; exact bytes are independently read back and registered as quarantined/pending before one durable job is linked. Endpoint/external-event replay is idempotent. Invalid signatures, stale timestamps, revoked credentials, disallowed event types, malformed JSON, unavailable connections, or absent active schemas create no delivery or job.

The responsive `/integrations` workspace exercises healthy, degraded, rate-limited, dead-letter, and disconnected states plus connection/schema, sync/receipt, signed-webhook, and provider-catalog views. Every visible control changes state or publishes an inspectable status. Desktop, tablet, and mobile captures were visually inspected at original or review resolution; the provider matrix, control boundaries, dense hashes, and live gate remained legible with no observed clipping, collision, or measured document-width overflow. The React best-practices review retained abortable production fetching, semantic controls, explicit loading/error/empty states, and no unnecessary memoization or client bundle dependency.

The preserved deterministic artifacts also passed inspection. The PDF remains six Letter pages and 11,602 bytes; every page was rendered and visually inspected with no observed clipping, overlap, broken glyph, or missing header/footer structure. The ZIP remains 17 entries and 51,723 bytes and passed CRC/content inventory. Its embedded PDF hash matches the standalone SHA-256 `6b8f29f6c8e41a725dfe37d79831f093c9c60c3c042306dd494856e8da5bea21`; the evaluator manifest hash remains `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`.

The first focused browser invocation reached an existing standalone build and correctly returned the not-found route for the new page. After the required production rebuild, the identical focused integration suite passed 3/3. The final consolidated release command rebuilt again and passed all 51 scenarios; the stale-build result is not counted as validation. The initial dependency audit inside the restricted sandbox could not resolve the npm registry and was rerun with approved network access, where it reported 0 vulnerabilities.

All connections, provider records, health checks, webhook events, schemas, receipts, and external responses are deterministic fictional fixtures. No live Graph, Gmail, Drive, AMS, property, model, or verifier implementation or credential is configured. Customer schemas, provider data rights, webhook senders, throughput, provider SLAs, managed secrets, staging operation, monitoring, rollback, semantic mapping acceptance, customer use, and external validation remain absent. M11 is locally validated but neither deployment-, customer-, market-, programme-, nor provider-validated. M12 operational hardening and launch validation are next.

Publication used branch `codex/m11-production-integrations` stacked on refreshed `origin/codex/m10-programme-analytics-recognition-graph` at `91efe015517ac3a28bc02f9352ce61d028c75cba`. The initial implementation head and remote SHA both resolved to `10c52242b8326347f06b86f5c08ddd072278e510`; ahead/behind was `0/1`; `git diff --check` passed; and local `git merge-tree --write-tree` produced `453d03a60add18af24c404905c9edea17a46e91a` without conflicts. GitHub draft PR [#18](https://github.com/dizzy1900/Fortify/pull/18) readback reported the intended base/head and `MERGEABLE`. The separate `UNSTABLE` aggregate reflected queued/running checks, not a merge conflict. This publication record follows that initial readback.

## Replacement north-star M10 programme administration and tenant analytics local-validation addendum

The M10 release surface passed ESLint, strict TypeScript, 22 Vitest files/82 tests, a 382-file secret scan, the 29-page/123-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 48/48 serial Playwright scenarios across desktop Chromium, an 834-pixel tablet, and Pixel 7. Production schema generation reported 180 tables and no drift; the migrated database exposed exactly 595 unique guards/triggers. The deny-by-default registry covers all 176 resource classes. `npm audit --audit-level=moderate` separately reported 0 vulnerabilities.

The M10 migration adds 12 tenant-owned tables for programme cohorts and immutable versions/membership decisions; versioned analytics policies, rights/privacy reviews, and publication; audit-derived recognition-graph events; human-confirmed workflow baselines; immutable programme metric snapshots; exact reports and artifacts; and exact query receipts. Database guards independently enforce tenant equality, published programme/profile pins, property/case/project/submission lineage, immediate version ancestry, separate author/reviewer/publisher decisions, policy lifecycle, clean exact stored bytes, immutable history, and tenant-only query execution.

`ProgrammeAnalyticsService` activates a cohort only from exact independently published programme and target-profile versions and an allowed property class. Human membership decisions append applicant, qualified, ineligible, insufficient, project-started/completed, removal, and correction events without overwriting history. Recognition-graph events derive their subject from an exact same-tenant audit event, bind optional property/case/project/programme/submission context, preserve a deterministic event hash, and create provenance rather than authority or causality.

The descriptive dataset reads normalized M6–M9 records for cohort membership, eligibility, projects, verified findings, exact submission delivery, separately categorized market responses, maintenance roll-forward, stakeholder benefits, capital stacks, and audit activity. It retains explicit denominators and nulls, labels case-to-delivery and delivery-to-response as elapsed time, and produces no hazard score, eligibility prediction, acceptance prediction, premium attribution, claims model, opaque ROI, or caused programme effect.

Analytics policies record mode, contract reference, opt-in, minimum cohort, de-identification, suppression, allowed metric families, retention, and deletion treatment. A separate rights/privacy reviewer and publisher are required. The current execution path is intentionally more restrictive than the schema: both the service and database reject `cross_customer_opt_in` snapshots and query receipts because a tenant policy is not cross-tenant authority. The privacy attack tests confirm that no benchmark or customer-identifiable aggregate ran.

Brokerage workflow reports require a human-confirmed baseline. Baseline measures and observed tenant measures remain separate. Brokerage-workflow and programme-outcome JSON/CSV artifacts are written behind the storage adapter, independently read back for exact size and SHA-256, stored with the adapter-reported encryption mode, scanned as internally generated clean bytes, and exposed through authenticated exact-byte export routes. Each report pins the programme and profile's exact governed source versions. M4 source-change impact now identifies affected reports and explicitly requires governed regeneration; it never mutates a report automatically.

The responsive `/programme-intelligence` workspace exercises active, insufficient-evidence, maintenance-review, and privacy-blocked scenarios plus programme, recognition-graph, analytics/report, and rights/privacy views. All visible controls work. Desktop, tablet, and mobile screenshots were inspected at original resolution; the institutional hierarchy, dense ledgers, state boundary, graph chain, report separation, and privacy stop remained legible with no observed clipping, collision, or measured document-width overflow.

The first complete M10 browser invocation passed 45 scenarios but failed the three source-impact expectations because the new report item was present in the accessibility tree while a strict visibility assertion targeted the list label rather than the visible impact summary. The expectation was changed to the visible report count and exact source-lineage boundary; the focused 3/3 source-impact rerun passed. The final complete 48/48 rerun is the release evidence.

All programme sponsors, membership decisions, source rules, graph events, baselines, metrics, benefits, reports, query receipts, and displayed outcomes are deterministic fictional fixtures. No customer supplied the baseline; no cross-customer contract or authority exists; no real programme participant, sponsor, insurer, broker, or customer used the workflow; and no managed deployment or external validation evidence exists. M10 is locally validated but neither deployment-, customer-, market-, nor programme-validated. M11 production integrations remain next.

Publication used branch `codex/m10-programme-analytics-recognition-graph` stacked on refreshed `origin/codex/m9-recognition-submissions-outcomes` at `89cf9c6e95d2a408c24de726367a20fd87c9e872`. The initial implementation head and remote SHA both resolved to `e903543ba2b254baaf0062a51349f83452f36f2b`; ahead/behind was `0/1`; `git diff --check` passed; and local `git merge-tree --write-tree` produced `0e6e2521c991e9b777e1620e91b0086019d5b8b6` without conflicts. GitHub draft PR [#17](https://github.com/dizzy1900/Fortify/pull/17) readback reported the intended base/head and `MERGEABLE`. The separate `UNSTABLE` aggregate reflected queued/running checks, not a merge conflict. This publication record follows that initial readback.

## Replacement north-star M9 recognition-submission and separated-outcome local-validation addendum

The M9 release surface passed ESLint, strict TypeScript, 21 Vitest files/80 tests, a 362-file secret scan, the 28-page/112-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 45/45 serial Playwright scenarios across desktop Chromium, an 834-pixel tablet, and Pixel 7. Production schema generation reported 168 tables and no drift; the migrated database exposed exactly 533 unique guards/triggers. The deny-by-default registry covers all 164 resource classes. `npm audit --audit-level=moderate` separately reported 0 vulnerabilities.

The M9 migration adds 15 tenant-owned tables for recognition-submission bindings and exact model-mapping snapshots; delivery attempts and exact receipts; scoped reviewer sessions; clarification requests and responses; independent evidence, model, rating, underwriting, placement, and funding event ledgers; case closure; and maintenance roll-forward. Database guards independently enforce tenant equality, submission/version/case/property lineage, allowed scoped reviewers, restricted revocation, correction ancestry, category-specific source and authority rules, later-renewal same-property roll-forward, and immutable governed history.

`RecognitionSubmissionService` starts with an already human-confirmed immutable packet version and independently reads every stored artifact byte back against its recorded size and SHA-256. It then binds the exact playbook, published target profile, optional published commitment, model-mapping states, requested action, destination, readiness blockers/caveats, and human confirmation. A blocker prevents preparation. Idempotent replay returns the same binding instead of creating a second governed version.

Delivery preserves failed attempts and immediate retry lineage. A successful deterministic fixture delivery stores the exact receipt bytes behind the storage adapter, independently reads size/hash back, and records provider/source custody separately from recipient acceptance. The provider explicitly reports fixture custody and no live counterparty acknowledgement. Exact replay returns the same delivery and receipt.

An external reviewer receives an opaque digest-only, expiring, reason-revocable session pinned to one case and one submission version. The review workspace cannot expose another submission, case, project, funding record, or tenant. Clarification retains original wording separately from normalized reason; broker responses preserve original wording and exact evidence-version links. Revocation is one-way and retained in history.

Evidence acceptance, model response, rating treatment, underwriting treatment, placement response, and funding response remain six separate append-only taxonomies. Corrective events cite their predecessor without overwriting it. Positive rating treatment requires an exact governed source. External review authority does not create funding authority, and absent treatment remains absent rather than being inferred from another category. Closure can explicitly remain `outcome_pending`; maintenance roll-forward requires a later renewal for the same property and human review, and never auto-approves evidence.

The responsive `/recognition` workspace exercises blocked, confirmed, delivered, clarification, accepted, corrected, pending, revoked, and maintenance states with functional submission, secure-review, decision, and maintenance controls. Desktop, tablet, and mobile screenshots were inspected with no observed clipping, collision, or measured document-width overflow. The first complete browser invocation exposed only a case-sensitive expectation mismatch between the test label and the rendered explicit `Unknown — No determination supplied` state; the expectation was corrected, the focused 3/3 scenario passed, and the complete 45/45 suite passed.

The preserved regression artifact remained exact: the PDF is six US Letter pages and 11,602 bytes; the ZIP is 51,723 bytes with 17 expected entries; and the deterministic evaluator reports manifest semantic hash `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`. All six PDF pages were rendered and inspected without observed overlap, clipping, broken glyphs, or missing page structure. `unzip -t` confirmed all 17 entries without compressed-data errors, and the content inventory was inspected separately.

All M9 organizations, destinations, delivery attempts/receipts, reviewer identities, correspondence, response events, case closure, and maintenance roll-forward are deterministic fictional fixtures. No recipient received bytes; no external reviewer authenticated; no insurer, MGA, model provider, rating authority, underwriter, placement market, lender, or funder made a decision; and no customer, managed deployment, or external validation evidence exists. M9 is locally validated but neither deployment-validated nor externally validated. M10 programme administration, portfolio analytics, ROI measurement, recognition-graph instrumentation, and data-right controls remains next.

Publication used branch `codex/m9-recognition-submissions-outcomes` stacked on refreshed `origin/codex/m8-model-mapping-commitments` at `d796cb37ad5f804595b1eda558d5ca1ee3609516`. The initial implementation head and remote SHA both resolved to `70d59d0791a64697f0e9685f2f01051eb2d2259c`; ahead/behind was `0/1`; `git diff --check` passed; and local `git merge-tree --write-tree` produced `36a79fa3ab327ba976da20bbd1e89601890ba4ba` without conflicts. GitHub draft PR [#16](https://github.com/dizzy1900/Fortify/pull/16) readback reported the intended base/head and `MERGEABLE`. The separate `UNSTABLE` aggregate reflected queued/running checks, not a merge conflict. This publication record follows that initial readback.

## Replacement north-star M8 model-mapping and market-commitment local-validation addendum

The consolidated M8 release surface passed ESLint, strict TypeScript, 20 Vitest files/78 tests, a 343-file secret scan, the 27-page/101-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 42/42 serial Playwright scenarios across desktop Chromium, an 834-pixel tablet, and Pixel 7. Production schema generation reported 153 tables and no drift; the migrated database exposed exactly 454 unique guards/triggers. The deny-by-default registry covers all 149 resource classes. `npm audit --audit-level=moderate` separately reported 0 vulnerabilities.

The M8 migration adds 17 tenant-owned tables for external providers/models/versions with exact source, rights, scope, definitions, review and publication; human-confirmed external outputs; evidence-bound input mappings with immutable pre/proposed values, reviews, exact evidence links, and append-only acceptance history; and recognition organizations plus explicit versioned commitments. Database guards independently enforce tenant equality, published/current/rights-reviewed sources, immediate lineage, three-human publication, approved conforming findings, exact property evidence, input/model consistency, supported review, confirmed state transitions, proposed-versus-accepted value separation, review-only authority, and immutable governed history.

`ModelRecognitionService` and 12 authenticated APIs preserve the same boundaries above the database. The responsive `/model-recognition` workspace demonstrates internally reviewed, submitted, accepted-with-modification, rejected, unsupported, expired, and no-commitment states; exact model/right/definition records; a visible pre-to-proposed-to-accepted chain; decision evidence; and a review-only commitment whose “will review” language never becomes “will insure.” Desktop, tablet, and mobile captures had no observed clipping, collision, or measured document-width overflow.

Five focused M8 contracts cover human-confirmed external outputs, separate model actors, exact accepted-value preservation, unsupported fail-closed behavior, review-only commitment authority, and database-level cross-tenant rejection. The preserved six-page PDF rendered cleanly on every page. The 17-entry ZIP passed CRC and inventory inspection; deterministic evidence remained 11,602-byte PDF, 51,723-byte ZIP, and manifest SHA-256 `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`.

The first post-change full browser attempt found a strict-locator ambiguity only in the new M8 test; the selector was narrowed and the focused scenario passed. A later concurrent tablet/mobile diagnostic interfered through the shared deterministic database and was discarded. Clean reset, serial project runs passed 14/14 desktop, 14/14 tablet, and 14/14 mobile. No product failure is hidden in the final count.

All displayed model providers, model definitions, mappings, external responses, recognition organizations, and commitments are fictional deterministic fixtures. Local validation does not establish model accuracy, semantic mapping validity, provider authorization, source redistribution rights, contract enforceability, underwriting/rating/financial authority, insurance acceptance, managed deployment, or customer reliance. M9 recognition submission and outcome separation remains incomplete.

Publication used branch `codex/m8-model-mapping-commitments` stacked on refreshed `origin/codex/m7-independent-verification` at `62cc6239bdd04682b6a3d746de3c61f3150064dc`. The initial implementation head and remote SHA both resolved to `b7bec667ac7c1f1903f8597da97df1019f559477`; `git diff --check` passed; the branch was 0 behind and 1 ahead; and local `git merge-tree --write-tree` produced `d8e4d6fbb82cb9af6f1dc928d0a90f85adb98817` without conflicts. GitHub draft PR [#15](https://github.com/dizzy1900/Fortify/pull/15) readback reported base `codex/m7-independent-verification`, head `codex/m8-model-mapping-commitments`, the initial implementation commit, and “No conflicts with base branch.” This publication record follows that initial readback.

## Replacement north-star M7 independent-verification local-validation addendum

The consolidated M7 release surface passed ESLint, strict TypeScript, 19 Vitest files/73 tests, a 322-file secret scan, the 26-page/89-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 39/39 serial Playwright scenarios across desktop Chromium, an 834-pixel tablet, and Pixel 7. Production schema generation reported 136 tables and no drift; the migrated database exposed exactly 386 unique guards/triggers. The deny-by-default registry covers all 132 resource classes. `npm audit --audit-level=moderate` separately reported 0 vulnerabilities.

The M7 migration adds 17 tenant-owned tables for verifier organizations and identities; versioned credentials and independent reviews; assignment-scoped access; signed conflict declarations; versioned methods; signed findings and exact evidence links; independent finding reviews; exceptions and append-only corrective actions; certificates and append-only issue/expiry/revocation/reinstatement events; maintenance obligations and evidence-refresh events; and longitudinal property-condition events. Database guards enforce tenant equality, credential/verifier identity, reinspection project lineage, finding assignment/method/intervention/profile integrity, restricted assignment revocation, and immutable governed conclusions/history.

`VerificationService` requires a human member to register credentials, a separate source-checking reviewer, a current credential, a published profile, reviewed project interventions, an explicit verifier-scope allowlist, and an expiring digest-only `fverify_` token. The external verifier resolves to exactly one assigned project and cannot read the capital stack. A signed no-conflict declaration is required before methods or findings.

Every finding pins the assignment, profile version, criterion, project intervention, method/version/date/limitations, exact same-property evidence versions, conclusion, verifier identity, signature hash, and one of nine impact/treatment evidence levels. The impact/treatment taxonomy is stored separately from the existing proof-strength taxonomy. Missing evidence, a stale credential, an unresolved conflict, a nonconforming or unreviewed finding, an unresolved exception, or collapsed finder/reviewer/issuer roles blocks certificate issuance.

Exceptions remain immutable; corrective actions supersede without deleting; reinspections preserve same-project predecessor lineage. Certificates bind the exact approved signed finding set and preserve issue/revocation history. Certificate-linked maintenance obligations require exact intervention versions; refresh/satisfaction events require exact evidence versions; absent future observations remain absent rather than inferred.

Sixteen authenticated APIs expose organization workspace, verifier/credential governance, assignment and assignment-filtered external workspace, conflict, method, finding/review, exception/corrective action, certificate/event, maintenance, and longitudinal property-condition operations. The responsive `/verification` workbench exercises populated/loading/error/empty plus verified, open-exception, expired-credential, insufficient-evidence, revoked-certificate, and no-assignment states. Desktop, tablet, and mobile screenshots were inspected with no observed clipping, collision, or document-width overflow.

All verifier organizations, credentials, registry sources, assignments, methods, geolocation, observations, findings, exceptions, corrections, certificates, and maintenance events are deterministic fictional fixtures. Fortify records workflow and provenance and does not represent itself as the substantive verifier. No real credential, rights-cleared registry, physical condition, qualified verification, compliance, designation, hazard reduction, insurer treatment, pricing outcome, programme acceptance, managed deployment, customer reliance, or external validation has been established.

Publication used branch `codex/m7-independent-verification` stacked on refreshed `origin/codex/m6-funding-project-execution` at `f1d6627e27d6511417b1566092b89eba818dbe0a`. The initial implementation head and remote SHA both resolved to `274359a4e260b2cc3e4b8342e9a4c120b4206777`; `git diff --check` passed; and local `git merge-tree --write-tree` produced `ab0bd627dd8845a436b8a514752e8a5028c39e9b` without conflicts. GitHub draft PR [#14](https://github.com/dizzy1900/Fortify/pull/14) readback reported base `codex/m6-funding-project-execution`, head `codex/m7-independent-verification`, the initial implementation commit, and “No conflicts with base branch.” This publication record follows that initial readback. M8 model mapping and commitments is the next incomplete milestone; M7 findings must not be mistaken for model acceptance, market commitments, or outcome authority.

## Replacement north-star M6 funding/project-execution local-validation addendum

The final consolidated M6 release command exited 0: ESLint, strict TypeScript, 18 Vitest files/69 tests, a 299-file secret scan, the 25-page/73-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 36/36 serial Playwright scenarios across desktop Chromium, an 834-pixel tablet, and Pixel 7. Production schema generation reported 119 tables and no drift; the migrated database exposed exactly 325 unique guards/triggers. The deny-by-default registry covers all 115 resource classes. `npm audit --audit-level=moderate` separately reported 0 vulnerabilities.

The final consolidated command used approved localhost binding and passed end to end, including 36/36 serial Playwright scenarios. An earlier sandboxed command had passed every gate through build, evaluation, and claims scanning before local server binding was denied with `listen EPERM 127.0.0.1:3000`; it is retained as environment evidence but is not the final release result. One duplicate invocation collided with a still-running build lock and is likewise not counted as a test or product failure.

The M6 migration adds 19 tenant-owned tables for stable funding programmes; immutable immediate-successor programme versions, deterministic rules, reviews, and publications; eligibility assessments/results; prepared applications; capital stacks/contributions; commitments and append-only events; milestones, dependencies, and append-only events; payment approvals; export-only instructions; project collaborator assignments; and stakeholder benefit-ledger entries. Database guards reject cross-tenant references, invalid programme lineage, self-review/publication collapse, unpublished eligibility, mismatched application assessments, scenario/project or payment/project mismatches, cross-project or forward milestone dependencies, mutable governed history, and anything except the fixed `not_executed_export_only` disbursement state.

`FundingProjectService` requires exact published-source programme pins, bounded rules, explicit evidence/payment/maintenance conditions, separate author/reviewer/publisher decisions, and a human-confirmed application record. Deterministic assessment preserves `eligible`, `ineligible`, and `insufficient_evidence` without a hidden score; an eligible result explicitly returns `programmeAwarded=false`.

Capital-stack creation requires at least two sources and rejects duplicate source references, duplicate programme versions, contributions above project cost, missing eligible programme assessments, and published award/cost-share ceiling breaches. Contributions distinguish owner, grant, financing, insurer, reinsurer, local-government, and philanthropic sources. No bank account, custody, settlement, or fund-transfer field or provider call exists.

Commitments preserve proposed, approved, corrected, and cancelled event history. Proposer and approver are separate; correction/cancellation references the latest event. Milestones preserve ordered same-project dependencies and block approval until predecessor milestones are approved. External property-manager, board, and contractor assignments use role allowlists, project scope, expiry, random digest-only tokens, and one-way revocation. The contractor fixture can submit milestone evidence but a direct capital-stack read fails authorization.

Payment recommendation requires a payment-eligible approved milestone and effective approved/corrected commitment. Export requires a second human and produces an exact canonical instruction payload/hash only; the test and audit event both report `movesFunds=false`. The stakeholder ledger retains expected cost, contribution, evidence level, source, timeframe, uncertainty, commitment, and realized response separately and produces no aggregate ROI claim.

Fifteen authenticated funding routes expose the organization workspace; project-filtered external workspace; programme version/review/publication; eligibility; application; stack; commitment/event; project execution/assignment revocation; milestone event; payment approval; and disbursement export operations. Project access tokens use a dedicated prefix, retain only digests, and resolve through the same authentication boundary. The responsive `/funding` workspace exercises populated, loading, error, empty, eligible, ineligible, insufficient-evidence, capital-source, commitment-history, milestone, export-boundary, collaborator-revocation, and stakeholder-ledger states. Desktop, tablet, and mobile captures were inspected with no observed clipping, collision, or document-width overflow.

The programme, sponsor, eligibility facts, project cost, contributions, approvals, milestones, users, and export are deterministic development fixtures. They are not a real funding rule, sponsor commitment, customer cost, verified physical completion, payment recommendation from an external funder, transfer, settlement, customer use, or programme validation. M6 is locally validated but neither deployment-validated nor externally/programme validated.

The preserved artifact regression remained deterministic: the PDF is six US Letter pages and 11,602 bytes with SHA-256 `6b8f29f6c8e41a725dfe37d79831f093c9c60c3c042306dd494856e8da5bea21`; the ZIP is 51,723 bytes, contains 17 entries, and retains manifest semantic hash `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`. Final PDF-page and ZIP-content inspection is recorded separately below; M6 does not alter those artifact bytes.

Publication used branch `codex/m6-funding-project-execution` stacked on refreshed `origin/codex/m5-profiles-capital-planning` at `e63887896af2c286f0b73b1df007183ff6a06fc4`. The initial implementation head and remote SHA both resolved to `ecea5235350056d5fe8d8aac241e7366be124a40`; ahead/behind was `0/1`; `git diff --check` passed; and local `git merge-tree --write-tree` produced `c550bf16084719ad4bdf6e5d03b9534a0a6538f9` without conflicts. GitHub draft PR [#13](https://github.com/dizzy1900/Fortify/pull/13) readback reported base `codex/m5-profiles-capital-planning`, head `codex/m6-funding-project-execution`, the initial implementation commit, and “No conflicts with base branch; merging can be performed automatically.” This publication record follows that initial readback. M7 independent verification is the next incomplete milestone; M6 milestone labels must not be mistaken for M7 verification conclusions.

## Replacement north-star M5 resilience-planning local-validation addendum

The consolidated M5 release gate exited 0: ESLint, strict TypeScript, 17 Vitest files/65 tests, a 276-file secret scan, the 24-page/58-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 22/22 serial Playwright scenarios across desktop Chromium and Pixel 7 passed. Production schema regeneration reported 100 tables and no drift; the migrated database exposed exactly 246 unique guards/triggers. The deny-by-default registry covers all 96 resource classes. `npm audit --audit-level=moderate` separately reported 0 vulnerabilities.

The M5 migration adds stable target profiles; immutable immediate-successor profile versions, minimum/preferred criteria, bounded applicability, independent review, and separate publication; stable interventions and immutable specifications/reviews; immutable property/profile baselines and criterion gaps; candidate resilience projects and reviewed intervention links; and transparent capital plans, scenarios, and scenario-project lineage. Tenant triggers reject cross-organization references, database guards duplicate author/reviewer/publisher separation and lineage checks, baseline creation requires a published profile, project use requires an independently approved intervention version, and governed authored/decision/scenario history rejects update/delete.

`ResiliencePlanningService` pins every profile to exact published governed-source versions and leaves the profile non-operative until independent review plus a third human publication decision. The executable evidence hierarchy preserves self-attested, documented, professional-observation, independent-verification, jurisdictional-record, programme-recognition, insurer-acknowledgement, modeled-analysis, and measured-outcome levels without collapsing them. Profile criteria distinguish minimum from preferred and record explicit verification methods and limitations.

Capital planning deterministically evaluates bounded applicability facts before creating an immutable baseline. It records satisfied, gap, insufficient-evidence, and not-applicable criterion states. Only independently reviewed intervention versions can compose candidate projects. Each scenario exposes cost range, duration, dependencies, maintenance, assumptions, funding-eligibility state, modeled-benefit state, insurer-treatment state, and rationale. The output is one of `options_available`, `inapplicable`, `insufficient_evidence`, or `no_attractive_path`; it contains no composite risk score or opaque ROI.

Seven authenticated APIs expose workspace readback, profile authoring/review/publication, intervention authoring/review, and capital-plan creation. The responsive `/resilience-planning` workspace exercises baseline, profile, intervention, scenario, applicable, inapplicable, insufficient-evidence, and no-attractive-path states. Desktop Chromium, 834-pixel tablet, and Pixel 7 captures were inspected with no observed clipping, collision, or document-width overflow.

The California profile, observations, interventions, costs, and scenarios are deterministic development fixtures. They are not rights-cleared technical standards, engineering advice, contractor bids, funding determinations, modeled benefit, insurer acknowledgement, customer approval, or physical-performance evidence. No managed deployment or external review has been validated. M5 is locally validated but neither deployment-validated nor externally validated.

Artifact regression remained deterministic: the PDF is six US Letter pages and 11,602 bytes with SHA-256 `6b8f29f6c8e41a725dfe37d79831f093c9c60c3c042306dd494856e8da5bea21`; the ZIP is 51,723 bytes, contains 17 CRC-valid entries, and retains manifest semantic hash `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`. Bundled Poppler emitted Fontconfig cache warnings but rendered all six pages successfully. Every page was inspected with no observed overlap, clipping, broken glyphs, missing headings, or missing footer/page structure.

Publication used branch `codex/m5-profiles-capital-planning` stacked on refreshed `origin/codex/m4-california-source-register` at `4531dcebfd9a87e2993a6740810b4a02a978b2c9`. The initial implementation head and remote SHA both resolved to `5c65c55df4f17acb1f67a4850bb81b551a33168d`; ahead/behind was `0/1`; `git diff --check` passed; and local `git merge-tree --write-tree` produced `aec0de1ab095138facda935c8d1d4f900066f94c` without conflicts. GitHub draft PR #12 readback reported base `codex/m4-california-source-register`, head `codex/m5-profiles-capital-planning`, one implementation commit, and “No conflicts with base branch; merging can be performed automatically.”

## Replacement north-star M4 governed-source local-validation addendum

The final release surface passed ESLint, strict TypeScript, 16 Vitest files/62 tests, a 262-file secret scan, the 23-page/51-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 20/20 serial Playwright scenarios across desktop Chromium and Pixel 7. Production schema regeneration reported 84 tables and no drift; the migrated database exposed exactly 196 unique guards/triggers. `npm audit --audit-level=moderate` reported 0 vulnerabilities. The initial consolidated run reached Playwright after every preceding gate passed but the managed sandbox denied `listen 127.0.0.1:3000` with `EPERM`; the identical browser suite was rerun with approved localhost binding and passed 20/20.

The M4 migration adds stable governed sources, immutable exact source versions, immutable independent reviews, immutable human publication decisions, immutable typed downstream dependencies, and append-only change alerts. Database guards reject cross-tenant source/storage/predecessor/reviewer/publication/dependency/alert references, dirty or hash-mismatched storage, non-immediate lineage, self-review, incomplete rights/source comparison, non-human publication, unpublished playbook source pins, and update/delete of governed history. The deny-by-default registry covers all 80 resource classes.

`GovernedSourceService` requires human members for authoring, review, and publication. Publication requires exact clean-byte/hash readback, an independent review, recorded rights and redistribution boundary, explicit source comparison, verify-current state, and human confirmation. Publishing a successor preserves its predecessor and dependencies, emits an immutable alert, and returns exact affected playbook versions and cases without mutating either. Profile and report impact states deliberately return `unavailable_not_implemented` until M5 introduces governed consumers.

Market playbook creation now requires one exact published, verified-current source version rather than caller-supplied source claims. It derives the immutable source snapshot, records a playbook-version dependency, rechecks source publication/currentness at review, and records an exact case dependency when linked. The responsive `/sources` workspace exercises candidate, blocked, review, publication, successor, and impact states with visibly synthetic metadata-only fixtures. Extraction may propose facts but cannot activate a rule.

The first focused mobile journey exposed intrinsic-width overflow and pointer interception in the impact summary. Grid/card min-width constraints, hash wrapping, and stacked mobile summaries repaired the defect. The final source journey passed on desktop and Pixel 7; both screenshots were inspected with no observed clipping, collision, or document overflow.

Artifact regression remained deterministic: the PDF is six US Letter pages and 11,602 bytes, the ZIP contains 17 CRC-valid entries and is 51,723 bytes, and the manifest semantic hash is `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`. All PDF pages were rendered with PDFium after bundled Poppler's Fontconfig cache was unavailable; headers, footers, notice fields, matrix, action register, hashes, caveats, and letter content were visually inspected without observed overlap, clipping, broken glyphs, or missing page structure.

The California publisher fixtures contain metadata and bounded summaries only. They are not archived rights-cleared source snapshots, legal review, proof of current requirements, customer source-policy approval, or operational change monitoring. No managed deployment or external review has been validated. M4 is locally validated but neither deployment-validated nor externally validated.

Publication used branch `codex/m4-california-source-register` stacked on refreshed `origin/codex/m3-live-brokerage-wedge`. Exact ancestry and ahead/behind checks passed, `git diff --check` passed, and local `git merge-tree --write-tree` produced `bdc16c3759af4f3c16a13e96641447ea3ae7ec26` without conflicts. The initial published implementation head and remote SHA both resolved to `cc817994b22e54d23011b378d869e6d9c1d6a718`. GitHub draft PR #11 readback reported base `codex/m3-live-brokerage-wedge`, head `codex/m4-california-source-register`, draft state, and “No conflicts with base branch; merging can be performed automatically.”

## Replacement north-star M3 live-brokerage local-validation addendum

The complete local release surface passed against the final lockfile: ESLint, strict TypeScript, 15 Vitest files/60 tests, a 250-file secret scan, the 22-page/46-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 18/18 serial Playwright scenarios across desktop Chromium and Pixel 7. Production schema regeneration reported 78 tables and no drift; the migrated database exposed exactly 168 unique guards/triggers. `npm audit --audit-level=moderate` reported 0 vulnerabilities. Final diff and merge-tree evidence are recorded at publication.

The M3 migration adds stable `evidence_requests`, immutable `evidence_request_versions`, and immutable `submission_artifacts`. Database guards independently reject cross-tenant case, external-principal, request-version, submission-version, and storage-object references. Request versions and artifacts reject update/delete; request lifecycle changes are constrained to the recorded state machine. The deny-by-default registry covers all 74 current resource classes, and the contract suite directly attacks every new table boundary.

`BrokerageCaseService` requires an authenticated organization member plus explicit human confirmation for governed request and packet actions. It enforces the exact case/assignment policy, binds request purpose/instructions/items/due date into an immutable version, records issue and expiry separately, and never implies that an off-platform request was delivered. Packet generation requires one case-linked notice carrying the five human-confirmed facts `market`, `policy`, `noticeDate`, `deadline`, and `requiredEvidence`, plus at least one governed request. Missing facts fail closed and no submission row persists.

The packet builder consumes only tenant-scoped normalized client/community/property/policy/case records, exact stored evidence bytes, confirmed notice facts and citations, request versions, and explicit contradiction counts. It does not import `DemoState`. Every evidence byte is independently size/hash checked before generation. The PDF, ZIP, manifest, and letter are written beneath the tenant prefix, independently read back, then recorded as clean internally generated objects, scan receipts, immutable artifacts, a confirmed submission version, audit event, and idempotency receipt in one transaction. Failure removes the newly written objects.

The end-to-end fixture begins with the real `PortfolioImportService`, not manually normalized graph rows. A fictional California CSV is suggested/mapped, scanned clean, previewed, human-confirmed, and committed into normalized client, community, property, two buildings, location, and policy records. Those exact records drive an appeal case, durable notice intake/classification/extraction, append-only human fact review, governed property-manager evidence request, exact evidence version, and immutable packet. Idempotent packet replay returns the same stored version and byte hashes.

Four authenticated production APIs expose brokerage workspace readback, request creation, request issue, and packet generation. The responsive `/brokerage` workspace exposes case/policy/property context, notice provenance and confirmation, request state and delivery boundary, human packet confirmation, and artifact type/size/hash. Its sandbox mode is visibly fictional and mutates only component fixture state. Production mode calls only the authenticated APIs and has no role switch or legacy global-state fallback.

Desktop 1280, tablet 834, and Pixel 7 screenshots were inspected. Initial inspection found policy date-only values shifted by the local time zone and the first mobile tab partially clipped; formatting was fixed to UTC and the mobile tabs changed to a four-column grid. The focused browser journey was rebuilt, rerun, and re-inspected 2/2 before the final 18/18 suite passed, with no measured document overflow.

The production fixture emitted a five-page PDF 1.7 on US Letter paper plus a four-entry ZIP containing the exact PDF, machine-readable manifest, exact accompanying letter, and exact evidence exhibit. All five pages were rendered at 150 DPI and inspected. The first render found a fixed-position footer extending beyond the right margin; it was changed to font-measured right alignment, regenerated, rerendered, and re-inspected. The final inspected PDF was 6,568 bytes with SHA-256 `0abc65e14af28dda5ad1c9367751f15392fff76bd01473d211ef67f0762c0133`; the ZIP was 8,531 bytes with SHA-256 `6cbdf1505d18f019f6f0150a59eaec456bce997a7839b8dd44cf0cf4c543e564` and passed `unzip -t`. These hashes identify this exact generated validation run; idempotent replay is guaranteed within its governed stored version, while fresh fixture runs create new governed record IDs.

No rights-cleared California brokerage export, managed PostgreSQL/PostGIS or RLS deployment, managed OIDC/MFA provider, private bucket/KMS/scanner, live external request delivery, ten-case production run, carrier/underwriter review, broker acceptance, or market outcome has been validated. M3 is locally validated but neither deployment-validated nor externally validated.

Publication used branch `codex/m3-live-brokerage-wedge` stacked on refreshed `origin/codex/m2-resilience-access-control`. Exact ancestry and ahead/behind checks passed, `git diff --check` passed, and local `git merge-tree --write-tree` produced `99eda38cf22b0a8ae3e62636cd3621b3e61bfb90` without conflicts. The initial published implementation head and remote SHA both resolved to `9eb70ab217d3cb16ba9fb070da000c2d693c031a`. GitHub draft PR #10 readback reported base `codex/m2-resilience-access-control` at `decdd43c0a31162c187ae39fb911852a0fdf8ace`, head `codex/m3-live-brokerage-wedge` at `9eb70ab217d3cb16ba9fb070da000c2d693c031a`, and `mergeable=true`.

## Replacement north-star M2 identity/secure-evidence local-validation addendum

The complete local release surface passed against the final lockfile: ESLint, strict TypeScript, 14 Vitest files/56 tests, a 237-file secret scan, the 21-page/42-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 16/16 serial Playwright scenarios across desktop Chromium and Pixel 7. Production schema regeneration reported 75 tables and no drift; the migrated database exposed exactly 157 unique guards/triggers. `npm audit --audit-level=moderate` reported 0 vulnerabilities. `git diff --check` passed before publication.

The M2 migration adds tenant-owned `portfolio_assignments` and immutable `data_access_logs`, and extends case assignments with purpose, data-domain, and reason-bound revocation fields. Database guards independently reject cross-tenant portfolio, membership, team, external-principal, and access-log portfolio/case references. Assignment content cannot be edited; exactly one reason-bearing revision increment may revoke it. Access-log update/delete is rejected. The deny-by-default registry covers all 71 current resource classes.

Seven exact resilience ecosystem roles join the compatibility matrix: property operator administrator, property manager, contractor evidence contributor, independent verifier, programme administrator, insurer/MGA reviewer, and lender/funder reviewer. Identity resolution includes only live direct case plus live direct/team portfolio assignments. Exact assignment permissions are unioned per scope and then intersected with the organization-role ceiling. Tests prove wrong-scope, missing, expired, revoked, and permission-exceeding requests fail closed; contractor, verifier, insurer/MGA, lender/funder, and programme boundaries reject prohibited operations.

`AccessControlService` validates tenant-owned scopes and active memberships, purpose length, explicit non-wildcard permissions, recognized data domains, role-domain limits, and future expiry. Creation and reason-bound revocation commit with audit events. Administrative workspace reads append immutable purpose-specific access records. Three authenticated production APIs expose tenant workspace readback, assignment creation, and revocation without a sandbox fallback.

The responsive `/access` workspace exposes organization context, security posture, live purpose grants, portfolio/case creation, revocation with retained history, an append-only access ledger, and plain-language separation-of-duty boundaries. Its sandbox mode is explicitly fictional and changes only local component state. Desktop 1280, tablet 834, and Pixel 7 screenshots were inspected. The first mobile matrix kept the hard-boundary column inside a horizontal scroll; it was repaired to labeled stacked rows, rebuilt, rerun, and re-inspected with no measured document overflow or observed clipping/collision.

The first focused browser invocation used the stale standalone build and correctly returned the route-not-found state; rebuilding exposed `/access`. The first rebuilt browser run then found an ambiguous text selector rather than a product defect; the selector was scoped and the exact scenario passed 2/2. The final complete browser run passed 16/16. A sandboxed localhost attempt was denied with `listen EPERM 127.0.0.1:3000`; the approved identical runs succeeded. The initial live registry audit found four moderate advisories in Drizzle Kit's legacy development loader chain. A narrow nested esbuild override removed them without changing Drizzle Kit; schema generation reported no drift, and the full gates were rerun against the resulting lockfile.

No managed OIDC/MFA provider, PostgreSQL/RLS policy, private object store/KMS/scanner, production secrets/rate limits, operational backup restore, or authorized customer/partner role workflow has been validated. MFA capability is metadata, not an enforced policy claim. The synthetic users and assignments are not real relationships or authority. M2 is locally validated but neither deployment-validated nor externally validated.

Publication used branch `codex/m2-resilience-access-control` stacked on refreshed `origin/codex/m1-california-property-graph`. Exact local ancestry, ahead/behind, `git diff --check`, and merge-tree preflights succeeded; the initial published head and remote SHA both resolved to `9509720e3672e0aaedb9cdc783a74d78bba51093`. GitHub draft PR #9 readback reported the intended base/head and `MERGEABLE`; the separate `UNSTABLE` aggregate reflected four queued CI checks and is not a merge-conflict result.

## Replacement north-star M1 property-graph local-validation addendum

One consolidated `npm run verify` invocation exited 0 with approved localhost binding: ESLint, strict TypeScript, 13 Vitest files/50 tests, a 225-file secret scan, the 20-page/39-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 14/14 serial Playwright scenarios across desktop Chromium and Pixel 7 passed. Production schema regeneration reported 73 tables and no drift; `npm audit --omit=dev` reported 0 vulnerabilities; `git diff --check` passed.

The M1 migration adds eight tenant-owned tables for property portfolios and membership, parcels, unit summaries, typed physical scopes, reviewed aliases, cross-property relationships, and immutable property snapshots. Exactly 147 database guards/triggers now enforce tenant ownership across every new reference, portfolio properties belonging to the portfolio client, unit-summary buildings belonging to the selected property, scope references to the same property, required scope targets, immediate version lineage, and version update/delete rejection. The deny-by-default registry covers all 69 current resource classes.

Every new graph record retains source system, optional source-record identifier, effective period, confidentiality state, one of ten contract-ready data-right classes, and a rights-recorded flag. Cross-customer use remains `prohibited` by default. The rights flag records the tenant's stated right for the specific source/use; it is not a Fortify legal determination. Consent, cohort, de-identification, suppression, and benchmark execution are explicitly unimplemented.

`PropertyGraphService` verifies tenant-owned parents, writes the graph plus audit event and idempotency receipt in one transaction, and exposes tenant-scoped readback. The two authenticated production APIs admit no sandbox fallback. The deterministic California fixture runs under `org-fortify-california-fixture`, separate from the preserved Colorado `org-fortify-sandbox`; exact replay is proven and a California-context read does not return Colorado records. Direct cross-tenant parcel and portfolio-link writes fail at the database layer. Property v2 without the immediate predecessor fails, and valid recorded versions cannot be updated.

The responsive `/property-graph` workspace exposes functional portfolio/property selectors and property, physical-scope, immutable-version, and rights/provenance views. Its two fictional California properties have two parcels with `geometry_status=unavailable`, null boundary payloads, six explicit scope nodes, reviewed aliases/relationship, and hash-bound version snapshots. Desktop 1280, tablet 834, and Pixel 7 mobile screenshots were inspected: tenant/source boundaries, insufficient spatial state, identity, rights, and caveats were legible with no measured horizontal overflow or observed clipping/collision.

The first otherwise-complete sandboxed validation reached Playwright and was denied a local bind with `listen EPERM 127.0.0.1:3000`; the approved identical full invocation then exited 0. The initial full test run also caught a stale expected resource count of 61 after the eight new classes were registered; correcting the inventory assertion made the all-resource tenant-mismatch test pass for 69/69 classes.

No rights-cleared California portfolio, approved parcel boundary, managed PostgreSQL/PostGIS deployment, RLS policy, geospatial query/performance result, backup/restore exercise, or customer validation is present. The nullable EPSG:4326-ready GeoJSON contract and PGlite trigger suite are local implementation evidence only. M1 is locally validated but is neither deployment-validated nor externally validated.

Publication used branch `codex/m1-california-property-graph` stacked on refreshed `origin/codex/resilience-os-foundation`. Exact local merge-tree preflight succeeded. GitHub draft PR #8 readback reported the intended base/head and `MERGEABLE`; the separate `UNSTABLE` status is check state and is not a merge-conflict result.

## Product-doctrine M0 local-validation addendum

The authoritative objective now defines Fortify as a California-first Resilience Investment and Insurance Recognition OS. Product doctrine, repository operating rules, architecture/data-model boundaries, milestone plan, evidence hierarchy, external-validation gates, and the empty-by-design California source register were reconciled. The public entry now presents the California product direction while labelling the existing Colorado renewal workflow as a fictional sandbox. A new prohibited-claims guard covers 18 direct guarantee, Fortify-certification/risk-score, automatic-recognition, and unsupported loss-reduction patterns in runtime source and generated text artifacts; its focused regression proves bounded language passes and a direct guarantee fails. The guard runs in local verification and both relevant CI jobs.

One consolidated `npm run verify` invocation exited 0 with approved localhost binding: ESLint, strict TypeScript, 12 Vitest files/45 tests, a 214-file secret scan, the 19-page/37-API production build, 12/12 deterministic sandbox checks, the 18-pattern claims scan, and 12/12 serial Playwright scenarios across desktop Chromium and Pixel 7 passed. Production schema regeneration reported 65 tables and no drift; `npm audit --omit=dev` reported 0 vulnerabilities; `git diff --check` passed.

The public doctrine page was captured and visually inspected at 1280-pixel desktop, 834-pixel tablet, and Pixel 7 mobile widths. California positioning, Colorado sandbox separation, authority boundaries, navigation/actions, workflow cards, trust statements, and disclaimers remained legible with no measured horizontal overflow or observed clipping/collision. The latest deterministic packet remained six Letter pages / 11,602 bytes and rendered without observed overlap, clipping, broken glyphs, or missing headers/footers. The 17-entry / 51,723-byte ZIP passed integrity checks, retained the manifest, letter, exact embedded PDF, and 14 exhibits; the embedded PDF hash matched the standalone PDF at `6b8f29f6c8e41a725dfe37d79831f093c9c60c3c042306dd494856e8da5bea21`.

The prior M1–M7 results below remain evidence for the isolated sandbox and reusable renewal foundations only. M0 does not prove a rights-cleared California portfolio/source register, target profile, intervention, capital plan, funding programme, independent verification, external-model mapping, explicit recognition commitment, production recognition submission, separated real market responses, programme administration, deployment, or external acceptance.

## Production transformation M7 local-implementation addendum

The versioned market-playbook and deterministic evidence-readiness milestone was locally validated on August 1, 2026. The final consolidated `npm run verify` exited 0 with approved localhost-bind permission after ESLint, strict TypeScript, 11 Vitest files/44 tests, a 209-file secret scan, the 19-page/37-API Next.js production build, 12/12 deterministic sandbox evaluation checks, and 10/10 serial Playwright scenarios on desktop Chromium and Pixel 7 profiles passed. `npm audit --omit=dev` reported 0 vulnerabilities, and production schema regeneration reported 65 tables with no drift.

The production migration adds six tenant-owned tables for stable playbooks, immutable scope/source versions, immutable requirement configuration, bounded applicability rules, independent review decisions, and append-only exact-version case links. Exactly 126 database triggers now enforce same-organization references, program/market and predecessor lineage, author/reviewer separation, verify-current approval, approved/effective case applicability, and immutable version/rule/review/link history. The deny-by-default registry covers 61 resource classes.

`MarketPlaybookService` creates a complete immutable version in one transaction with its audit event. Scope includes market, exact optional program, jurisdiction, peril, property class, optional policy form, and effective period. Sources retain name, URL, version, exact citation, verify-current state, and content hash. Requirement configuration retains required/recommended and blocker state, accepted evidence/source types, freshness, exact scope, review authority, deadline, template/delivery fields, caveat, and bounded conditions. Conditions accept only six named fields and four comparison/set operators; no arbitrary JavaScript, SQL, regular expression, or model-generated executable code is accepted.

Approval is a separate immutable human record. The database and service reject self-review and reject approval when the source has not been verified current. A reviewed version is not edited; a successor cites the immediately prior version, and deterministic diff output identifies scope, added, removed, changed, and condition changes without treating record metadata as a semantic change. Case linkage pins one applicable approved version to one destination and preserves the prior link. Zero matches fail closed; overlapping approved versions also fail closed for administrator resolution.

Readiness reports `ready`, `missing`, `stale`, `scope_mismatch`, `contradiction`, `unreviewed`, `insufficient`, or `not_applicable` per requirement. Accepted type/source/disposition, scope, freshness/expiry, and human-review state must be satisfied together by qualifying evidence; separate weak records cannot manufacture readiness. An unresolved blocking item always returns `blocked`, required gaps return `review_required`, and recommended-only gaps return `ready_with_caveats`. The response records `averageUsed=false` and explicitly states that averages cannot offset blockers. It is labelled submission evidence readiness and carries a caveat that it is not underwriting risk, compliance, acceptance probability, or an insurance outcome prediction.

Five authenticated APIs expose tenant workspace readback, immutable version creation, independent review, exact case evaluation, and append-only case linking. The responsive `/playbooks` workspace covers destination selection, fail-closed evaluation, exact-version pinning, bounded administrator authoring, source/citation verification, requirement selection, version lineage, hashes, independent review, and explicit caveats. The browser workflow inspected blocker output, authored a synthetic successor, approved it as a separate walkthrough actor, and verified the version history at desktop and mobile sizes. Initial mobile inspection showed the selected history tab scrolled the first label partially out of view; tab sizing was tightened, rebuilt, rerun, and re-inspected with all labels visible and no document-width overflow.

The first sandboxed `npm run verify` completed lint, typecheck, all 44 tests, secret scan, build, and deterministic evaluation, then the managed sandbox denied the Playwright standalone server bind with `listen EPERM 127.0.0.1:3000`. A final complete invocation with approved localhost-bind permission exited 0, including the exact full 10-scenario suite. The earlier denial is recorded only as an execution-environment limitation.

The playbook sources and destination shown in the sandbox are fictional. No rights-cleared brokerage or market guide, carrier reviewer, managed PostgreSQL deployment, operational author/reviewer process, or external destination acceptance has been validated. M7 is implemented locally but is neither deployment-validated nor externally validated. The universal percentage remains only in the preserved fictional sandbox and deterministic artifact regression; production readiness uses the new exact destination service.

## Production transformation M6 local-implementation addendum

The durable document-processing milestone was locally validated on August 1, 2026. ESLint, strict TypeScript, 10 Vitest files/41 tests, a 192-file secret scan, the 18-page/32-API Next.js production build, 12/12 deterministic evaluation checks, and 8/8 serial Playwright scenarios on desktop Chromium and Pixel 7 profiles passed. `npm audit --omit=dev` reported 0 vulnerabilities, and production schema regeneration reported no drift.

The production schema now applies 59 normalized tables and exactly 100 triggers. Six new tables retain durable jobs, immutable attempts, extraction runs, multiple candidate fields, append-only human reviews, and superseding fact versions. Source documents now bind clean storage objects, versions, supersession, and classifier evidence; source passages retain extraction run, page, segment, kind, and optional normalized region/rotation. The deny-by-default registry covers 55 resource classes, and direct database guards reject cross-tenant job, attempt, run, passage, candidate, review, and fact references.

`DocumentPipelineService` admits only independently scanned clean objects, independently reads and verifies exact bytes and hashes, records idempotent source/job state, leases work to separately scoped service accounts, recovers stale leases, schedules bounded retries, and records dead-letter failures. Manual retry requires a human reason and adds only one bounded attempt. Attempts, runs, passages, candidates, reviews, and facts cannot be silently rewritten. Worker/service-account principals can extract candidates but cannot confirm facts.

The provider/classifier/extractor framework stores stable keys and versions, classifies nine correspondence families, and extracts carrier/sender, policy, dates/deadlines, carrier-stated classifications, stated drivers, requested mitigation/evidence, appeal rights, communication history, and reason codes without creating a Fortify risk score. Multiple candidates, low confidence, conflicts, missing fields, unavailable geometry, and model-derived values remain explicit. Human confirmation creates a fact; correction creates a superseding fact version; rejection creates no fact.

Four authenticated APIs expose workspace readback, intake, field review, and dead-letter retry. The responsive `/documents` workspace covers quarantine upload, clean-object selection, durable processing state, retries, document/candidate filters, pagination, exact source citation, model-derived disclosure, human confirmation/correction/rejection, and full fact/review history. The synthetic browser workflow confirmed and corrected a policy fact, confirmed a low-confidence model-derived candidate, retried a dead-letter job, queued a new job, and preserved the processed source for final desktop/mobile inspection. Both viewports passed without document overflow or observed clipping.

The default production adapter is deliberately offline and deterministic: plain text and selectable PDFs only. It reports unavailable native PDF geometry instead of fabricating it. Scans, rotations, tables, images, conflicts, and model-derived extraction are covered by exact-hash fixtures and an injected provider boundary, not a live OCR service. No rights-cleared customer correspondence, managed worker, external provider credential, provider license/data right, live accuracy study, retention/egress review, latency/cost evidence, or staging operation has been validated. M6 is implemented locally but is neither deployment-validated nor externally validated.

The deterministic artifact regression remained stable: the PDF is 6 Letter pages and 11,602 bytes; the ZIP has 17 entries and 51,723 bytes; its embedded PDF exactly matches the standalone PDF SHA-256 `6b8f29f6c8e41a725dfe37d79831f093c9c60c3c042306dd494856e8da5bea21`; and the manifest retains 14 evidence hashes, 2 mitigation actions, fictional-demo status, recorded human confirmation, and explicit non-score/non-outcome limitations. All six pages were rendered and visually inspected with no observed overlap, clipping, broken glyphs, or missing page/footer structure.

## Production transformation M5 local-implementation addendum

The portfolio/SOV import milestone was locally validated on August 1, 2026. ESLint, strict TypeScript, 9 Vitest files/37 tests, a 177-file secret scan, the 17-page/28-API Next.js production build, 12/12 deterministic evaluation checks, and 6/6 serial Playwright scenarios on desktop Chromium and Pixel 7 profiles passed. `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities.

The first consolidated `npm run verify` reached its final Playwright step and then the managed sandbox denied the standalone server bind with `listen EPERM 127.0.0.1:3000`. The exact six-scenario suite was rerun with the approved local-bind permission and passed. This was an execution-environment denial, not counted as a passing consolidated command.

The production schema now applies 53 normalized tables and exactly 72 triggers. Five new tables retain saved mappings, immutable mapping versions, import runs, row-level raw/normalized quarantine, and immutable preview/commit/rollback receipts. The deny-by-default registry covers 49 resource classes. Direct database guards reject cross-tenant mapping, clean-object, book, row, and receipt references.

The parser reads actual CSV bytes and XLSX workbooks with named sheets, non-default header rows, spreadsheet dates, quoted CSV fields, blank-row handling, file/row/column limits, address normalization, stable external identifiers, and explicit unit, year, date, and ISO-currency validation. The commit service does not merge solely by name: conflicting identifiers and normalized-address collisions remain ambiguous, rejected rows remain retained, and only explicitly human-confirmed accepted rows create normalized client, community, property, identifier, location, building, and policy records. Exact request/content hashes provide idempotent replay, and rollback marks only import-owned records while retaining history.

Seven authenticated production API routes expose organization-scoped workspace options, mapping suggestion from clean exact-readback bytes, immutable mapping save, preview, import readback, human-confirmed commit, and rollback. The responsive `/imports` workspace includes upload-to-quarantine, explicit scanner state, clean-object/book selection, mapping review, row-state filters, confirmation language, receipts, recent imports, and reason-bound rollback. Loading, error, empty, populated, ambiguous, rejected, committed, and rolled-back states are implemented. The sandbox route is visibly fictional and never persists the walkthrough.

The production standalone UI was exercised through the complete synthetic preview, quarantine review, accepted-row confirmation, receipt, and rollback sequence on desktop Chromium and Pixel 7. A first mobile run detected document overflow from a grid item's table min-content width; the grid/panel containment was repaired and the exact mobile scenario then passed without document overflow. Both final viewport screenshots were visually inspected: mapping fields, row states, receipt hashes, safety boundaries, and rollback language were legible with no observed collision or clipping.

The XLSX fixture was authored through the repository spreadsheet workflow, inspected for typed values and formula errors, rendered, and visually checked at both worksheet tabs. Applied Epic-compatible and AMS360-compatible CSVs are synthetic fixture boundaries only. No rights-cleared customer export, vendor certification, vendor API, screen scraping, or managed storage/scanner/database path has been validated. M5 is implemented locally but remains neither deployment-validated nor externally validated.

## Production transformation M4 addendum

The secure object-storage and evidence-upload milestone was locally validated on August 1, 2026. `npm run verify` exited 0 after ESLint, strict TypeScript, 8 Vitest files/31 tests, a 155-file secret scan, the production build, 12/12 deterministic evaluation checks, and 4/4 serial Playwright scenarios on desktop Chromium and Pixel 7 profiles. `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities.

The production schema now applies 48 normalized tables and exactly 61 triggers. M4 adds tenant-owned storage objects, expiring/revocable access grants, immutable malware-scan results, and immutable backup manifests/items. The deny-by-default registry now covers 44 resource classes. Direct database guards reject cross-tenant storage references, and service tests reject path traversal and cross-tenant object access.

The S3-compatible AWS SDK v3 adapter binds a private bucket key, explicit region, short expiry, size, MIME type, SHA-256, and AES256 or KMS settings into signed operations. Finalization independently reads checksum/metadata before quarantine. Byte checksum/size and basic content signatures are rechecked before a scanner result can promote an object; infected, scanner-error, and MIME-spoofed content fail closed. Only clean objects can create immutable evidence versions or download grants. Tests also prove grant revocation/expiry/one-time use, legal-hold and retention deletion blocks, exact-byte backup readback, and restore verification.

The build compiled the preserved 16 product pages and 21 API routes, including five protected production storage endpoints. M4 adds no new visible sandbox control; the unchanged sign-in and complete guided workspace were regression-tested across desktop and mobile, with all public/workspace routes healthy. The deterministic PDF/ZIP story remains byte-identical at 11,602 and 51,723 bytes, with manifest hash `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`.

No managed object-storage account or malware provider was configured. Private bucket and CORS policy, provider checksum semantics, KMS/credential scope, live malware behavior, lifecycle/object-lock enforcement, deletion operations, an independent backup destination, and a monitored disaster-recovery restore remain deployment-validation gates. The local exact-byte fixture backup is not evidence of managed backup or recovery. Live customer data remains prohibited.

## Production transformation M3 addendum

The identity, organizations, and authorization milestone was locally validated on August 1, 2026. `npm run verify` exited 0 after ESLint, strict TypeScript, 7 Vitest files/27 tests, a 142-file secret scan, the production build, 12/12 deterministic evaluation checks, and 4/4 serial Playwright scenarios on desktop Chromium and Pixel 7 profiles. `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities.

The production schema now applies 43 normalized tables and 53 triggers across the M2-M3 migrations. M3 adds 11 organization roles; OIDC discovery/authorization-code/PKCE/state/nonce; an explicit non-production local provider; opaque organization-bound sessions; email-bound invitations; membership/session revocation; team and case assignments; scoped service/API credentials; case-scoped external access; and customer-approved support grants. Only token digests persist for sessions, invitations, API credentials, and external grants.

The 9-test M3 suite iterates all 39 registered production resource classes and proves organization mismatch is denied. It also covers unknown/absent scope denial, direct database rejection for every new tenant-reference category, single-use OIDC attempts, safe return paths, invitation/session expiry, invitation replay, membership-triggered session revocation, API scope/revocation, external case scope/revocation, and support grant/revocation.

The build compiled 16 page routes and 16 API routes, including sign-in, OIDC/local/session/logout endpoints, protected production community GET/PATCH, and invitation create/revoke routes. The sign-in surface was inspected at desktop and 390 by 844 mobile sizes. The sandbox/production distinction, access-control assurances, evidence-infrastructure boundary, and no-outcome-guarantee language were visible and legible with no observed collision or horizontal overflow.

No live identity provider was configured. OIDC discovery, redirect registration, managed MFA enforcement, secrets, provider-admin lifecycle, staging rate limits, and defense-in-depth RLS remain deployment-validation gates. PGlite remains local PostgreSQL-compatible evidence rather than proof of the selected managed database. Secure object storage is M4 and is still absent, so live customer data remains prohibited.

## Production transformation M2 addendum

The normalized PostgreSQL and sandbox-isolation milestone was locally validated on August 1, 2026. `npm run verify` exited 0 after ESLint, strict TypeScript, 6 Vitest files/18 tests, a 120-file secret scan, production build, 12/12 deterministic evaluation checks, and 4/4 serial Playwright scenarios on desktop Chromium and Pixel 7 profiles. `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities.

The generated PostgreSQL migration creates 32 normalized tables and 43 triggers. Contract tests prove clean migration without `app_state`, organization-scoped repository operations, direct cross-tenant reference rejection, immutable audit/evidence/version records, optimistic concurrency, audit/domain-write atomicity, idempotent renewal-case replay/conflict handling, deterministic sandbox seed replay, and transaction rollback on a corrupted seed.

The production runtime requires an explicit mode and `DATABASE_URL`; production requests do not fall back to the SQLite `DemoState` routes. The existing fictional demo remains intentionally isolated as `org-fortify-sandbox`, synthetic, and not opted into cross-customer analytics.

The PostgreSQL tests use PGlite because Docker and native PostgreSQL are unavailable in this validation environment. PGlite exercises PostgreSQL SQL and trigger semantics but does not prove compatibility with the eventual managed provider, network controls, pooling limits, backup/restore, monitoring, or a deployed topology. Those remain explicit external gates. Production identity/resource authorization and secure object storage are also still absent, so live customer data remains prohibited.

The real guided-entry UI was re-inspected at 1440 by 720 and 390 by 844. The institutional layout, guided controls, fictional-data disclosure, role entry points, and demo manifest remained legible and usable with no observed responsive collision.

## Production transformation M1 addendum

The repository/CI/status foundation was locally validated on August 1, 2026. `npm run verify` exited 0 after running ESLint, strict TypeScript, 4 Vitest files/10 tests, a 101-file repository secret scan, the production build, 12/12 deterministic evaluation checks, and 4/4 serial Playwright scenarios on desktop Chromium and Pixel 7 profiles. `npm audit --omit=dev --audit-level=high` reported 0 vulnerabilities.

The new migration test applies the complete SQLite sandbox migration to a blank database and verifies 30 domain tables plus both audit immutability triggers. It is deliberately not evidence of the future production PostgreSQL migration.

The real application was inspected at 1440 px desktop, 768 px tablet, and 390 px mobile widths. There was no horizontal overflow and no browser console error. Inspection found a mobile top-bar collision between the route breadcrumb and role label; the label markup was corrected, rebuilt, and visually rechecked at 390 px.

GitHub checks subsequently ran on the stacked M5 PR. CodeQL/security passed. The container job fails before Trivy because `node:22-bookworm-slim` lacks the native build prerequisites required by `better-sqlite3`; the quality job reports three SQLite/Vitest workers exiting after test completion while PGlite tests pass. Those CI fixes require explicit approval and are not included in M6. GitHub branch protection and security settings still require owner action/readback. Production database/provider operation, identity/MFA, managed storage/scanning, managed document workers/providers, external integrations, backup/restore, and external validation remain incomplete.

## Measured gates

| Gate                     | Exact result                                                                                                                                                         |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Fresh-lockfile setup     | `npm ci` passed from `package-lock.json` with 459 packages installed                                                                                                 |
| Consolidated gate        | `npm run verify` exited 0                                                                                                                                            |
| ESLint                   | Passed                                                                                                                                                               |
| Strict TypeScript        | Passed with `tsc --noEmit`                                                                                                                                           |
| Unit/integration         | 3 files, 9 tests passed                                                                                                                                              |
| Production build         | Next.js 16.2.12 webpack build passed; 16 product routes and 7 API routes                                                                                             |
| Deterministic evaluation | 12/12 checks passed; reset digest matched `db9db21485615453`                                                                                                         |
| Guided browser demo      | 4/4 passed serially against the production standalone server: desktop Chrome and Pixel 7, full story plus all-route health                                           |
| Runtime dependency audit | `npm audit --omit=dev`: 0 vulnerabilities                                                                                                                            |
| Full dependency audit    | 4 moderate development-only findings remain in Drizzle Kit's legacy `@esbuild-kit` chain; npm proposes a breaking downgrade, so they are recorded rather than forced |
| Claims scan              | No prohibited promise/affiliation phrases found in seeded UI source                                                                                                  |
| Secret scan              | No credential-shaped assignments or bearer tokens found in repository source/configuration                                                                           |
| Diff hygiene             | `git diff --check` passed                                                                                                                                            |

The first hot-reload E2E attempt exposed reloads caused by runtime file writes, so the final suite was moved to the built standalone server. The production-server rerun passed without that dev-watch failure mode.

## Deterministic demo evidence

- Seed version: `fortify-demo-2026.08.01-v1`.
- Universe: 3 communities, 3 carriers, 42 evidence records, 28 requirements, and 6 broker-entered mitigation actions.
- Readiness totals: 87%, 75%, and 99%, each derived only from coverage, freshness, confidence, scope match, contradiction resolution, and human review.
- Edge cases: duplicate/conflict, expired evidence, incomplete case, returned clarification, carrier rejection with reason, successful fictional reconsideration, and year-over-year reuse.
- Human gates: all notice fields and exact submission contents are required before artifact generation.
- Roles: broker mutations, manager task/evidence-work mutations, and read-only underwriter evidence access are enforced server-side.

## Generated artifacts and inspection

| Artifact                                          | Result                                                                                                                  |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `artifacts/evaluation/demo-evaluation.json`       | Pass report, 12 checks                                                                                                  |
| `output/pdf/case-jefferson-submission-v1.pdf`     | Real PDF 1.7, Letter, 6 pages, 11,602 bytes, SHA-256 `6b8f29f6c8e41a725dfe37d79831f093c9c60c3c042306dd494856e8da5bea21` |
| `output/packets/case-jefferson-submission-v1.zip` | Real ZIP, 17 files, 51,723 bytes, SHA-256 `ed3a7e0414be5f13b12ac413afea9d4415e0823255c5231dfee919e3e0a3a8be`            |
| Manifest semantic hash                            | `47c5de9b8c2da8dfc040951b57697a2081fec8f1b3817e5148480aefaf9aef9a`                                                      |
| Screenshots                                       | Portfolio, generated packet, and maintenance/reuse views inspected at desktop size                                      |

The PDF and ZIP hashes were reproduced identically across two consecutive evaluator runs. All six PDF pages were rendered to PNG and visually inspected: headers/footers, notice confirmation, requirement matrix, mitigation-action register, evidence index, caveats, and editable-letter paragraph breaks are legible with no observed clipping or overlap. ZIP inspection confirmed `manifest.json`, the editable letter, the PDF, and 14 exhibits. JPEG exhibit signatures and SHA-256 readback are covered by tests.

## Manual rubric

| Dimension             | Result | Evidence                                                                                                                                                          |
| --------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Buyer relevance       | Pass   | Deadline triage, notice-to-packet workflow, assignments, clarification loop, and reuse align to the specialist-broker wedge                                       |
| Trust                 | Pass   | Fictional labels, source/version/verify-current status, human gates, explicit missing/conflicting evidence, immutable audit, and limitations remain visible       |
| Workflow completeness | Pass   | Nine guided steps complete from dangerous renewal through next-year reuse; every visible control has an implemented result                                        |
| Visual polish         | Pass   | Institutional light theme, restrained status colors, dense readable tables, local map, responsive layout, focus styles, print controls, and inspected screenshots |
| Unsupported claims    | Pass   | No Fortify-created risk score, compliance designation, premium forecast, carrier promise, or official IBHS affiliation                                            |

No critical manual-review issue remains.

## Primary-source review

- Colorado's signed HB25-1182 source was reviewed on August 1, 2026. It supports the July 1, 2026 effective date, covered homeowner/condominium/multifamily and FAIR Plan contexts, written annual notice concepts, 10-day appeal acknowledgement, and 30-day reconsideration decision. The app still labels this selected workflow content as verify-current and not legal advice.
- The objective supplied a December 2025 IBHS Multifamily reference. Current official review found a June 9, 2026 IBHS update introducing formal Multifamily and Neighborhood standards and updated Home requirements. Fortify preserves the supplied version as historical demo context, links the current update, and does not reproduce proprietary requirements or claim affiliation.

## Environment and limitations

- Docker/Compose files are included, but Docker was not installed in the validation environment; the equivalent Node 22 fresh-lockfile, standalone build, health route, and production-server flow were validated locally.
- Demo role switching is not authentication. Tenant isolation, production identity, Postgres, encrypted object storage, malware scanning, retention/legal hold, backups, monitoring, container scanning, and formal accessibility/legal review remain production gates.
- The map is illustrative local GeoJSON, not survey-grade. Notice intake has no OCR. The PDF is not tagged for accessibility. No email, carrier API, contractor marketplace, billing, remote sensing, or external delivery is connected.
- Carrier acceptance, renewal, insurability, discounts, appeal success, and pricing changes are not guaranteed.

## Customer questions for discovery

1. How many community-association renewals and appeals does the practice handle each year?
2. How long do evidence collection, reconciliation, and packet assembly take today?
3. Where do carrier requirements vary enough to break a reusable workflow?
4. How often is evidence missing, stale, ambiguous, duplicated, or scoped to the wrong parcel/building/community?
5. Will underwriters use the structured packet and provenance index in a live review?
6. Will the broker provide 10-30 redacted live cases for one renewal-cycle design-partner pilot?
7. Will the broker pay for workflow time, defensibility, and reuse independent of any premium outcome?
