# Tenant entry-point inventory

Measured on August 4, 2026 by `node scripts/inventory-tenant-entry-points.mjs`. This is a convergence ledger, not an assertion that C0 is complete.

## Current coverage

| Entry-point class                              | Count | Request-bound state                                                                                                                                                                                                                                |
| ---------------------------------------------- | ----: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authenticated production routes                |   120 | 109 access/brokerage/community/document/funding/integration/model-recognition/playbook/planning/portfolio-import/property-graph/recognition/membership/source/storage/verification routes bound; 11 still use an unbound service/database instance |
| Inbound production webhooks                    |     1 | Bound through an opaque endpoint-to-tenant bootstrap and transaction-local RLS context                                                                                                                                                             |
| Authenticated session routes                   |     2 | Session read and logout bound through the same authenticated tenant transaction                                                                                                                                                                    |
| Identity bootstrap routes                      |     3 | Local sign-in plus OIDC start/callback are bound through application-role and tenant-bootstrap transactions; local sign-in is development-only                                                                                                     |
| Tenant document workers                        |     1 | Bound through explicit organization/actor context and `withTenantTransaction`                                                                                                                                                                      |
| Total enumerated tenant-sensitive entry points |   127 | 116 bound; 11 unbound                                                                                                                                                                                                                              |

The production route families are access (3), brokerage (4), communities (2), documents (4), funding (15), integrations (10), memberships (2), model recognition (12), playbooks (5), portfolio imports (7), programme analytics (11), property graph (2), recognition (11), resilience planning (7), sources (5), storage (5), and verification (16). The inventory script emits every exact path and fails if a route cannot be classified.

## Bound slice

- Brokerage workspace read, evidence-request create, evidence-request issue, and packet generation resolve an opaque session or bearer credential, enter the non-owner `fortify_app` role, set transaction-local organization and actor context, revalidate the complete principal under RLS, and execute the domain service on the same checked-out transaction.
- Access workspace read, purpose-assignment create, and assignment revoke use the same request transaction. The administrative workspace selects only the fields its UI consumes; it does not read or return session hashes, IP/user-agent metadata, storage keys, request identifiers, audit write metadata, or unrelated team rows. A non-administrative membership is rejected after tenant-bound authentication.
- Storage upload request, quarantine finalization, download-grant issue, one-use redemption, and revocation use the same request transaction. Request-bound attacks reject cross-tenant issue and redemption, require exactly one winner under concurrent redemption, and prove upload/finalization responses omit direct object keys, provider/bucket/encryption identifiers, tenant identifiers, and audit write metadata. The signed operation URL remains purposefully available only for the authorized upload or download action.
- Membership invitation creation and reason-bound revocation use the same authenticated request transaction. Creation exposes only invitation and membership identifiers, expiry, delivery state, and the one out-of-band acceptance URL required by the caller; it does not expose a separate token, email, tenant identifier, token digest, or persistence metadata. A request-bound attack rejects non-administrative creation and cross-tenant revocation, then verifies the authorized revocation remains tenant-scoped and append-only.
- Community list/summary update and property-graph workspace/portfolio registration use the same request transaction. Their presenters expose only customer-facing property identity, scope, provenance, rights, spatial-state, summary, revision, and registration receipt fields; organization/client foreign keys, write metadata, lifecycle state, raw version snapshots, and unused source-record fields are omitted. Actual route attacks reject cross-tenant community and property registration, reject read-only mutation, preserve the other tenant unchanged, and prove the workspace contains only the authenticated tenant.
- Document workspace, clean-object intake, candidate review, and dead-letter retry use the same request transaction as authentication. The workspace projects only the case, clean-object, document, bounded-job, attempt, extraction, citation, candidate, review, and fact fields consumed by the UI; tenant identifiers, storage custody, worker/lease details, provider diagnostics, input hashes, and write metadata are omitted. Actual route attacks reject cross-tenant intake/review/retry, reject read-only mutations, preserve the other tenant unchanged, and prove minimized successful receipts.
- Portfolio-import workspace, clean-object mapping suggestion, immutable mapping save, dry-run preview, exact import readback, human-confirmed commit, and reason-bound rollback use the same request transaction as authentication. Presenters retain only onboarding options, normalized review rows, row states, count summaries, and receipt hashes/times consumed by the UI; raw source rows, tenant and storage custody, candidate/applied entity identifiers, idempotency/request hashes, receipt summaries, and persistence metadata are omitted. Actual route attacks reject cross-tenant suggestion/preview/read/commit/rollback, reject read-only mutations, preserve the other tenant unchanged, and prove minimized responses.
- Governed-source workspace, source registration, version staging, independent review, and publication use the same request transaction as authentication. Presenters retain only the source, version, review, publication, dependency, change-alert, doctrine, and bounded receipt fields consumed by the UI; organization identifiers, storage custody, persistence metadata, and unused lifecycle/revision fields are omitted. Actual route attacks prove tenant-only workspace data, reject read-only mutation and cross-tenant version/review/publication attempts, preserve the foreign tenant unchanged, and require distinct author, reviewer, and publisher identities.
- Market-playbook workspace, immutable version creation, independent review, deterministic case evaluation, and exact-version case linkage use the same request transaction as authentication. The workspace presenter retains only destination, requirement, governed-source pin, version, review, case, rule, and linkage fields consumed by the UI; organization identifiers, write metadata, lifecycle fields, internal actor metadata, and unused destination fields are omitted. Actual route attacks prove tenant-only workspace data, reject read-only creation/review/linking, reject cross-tenant creation/review/evaluation/linkage, preserve the foreign tenant unchanged, require an independent reviewer, and prove minimized successful receipts.
- Resilience-planning workspace, profile authoring/review/publication, intervention authoring/review, and capital-plan creation use the same request transaction as authentication. The workspace presenter retains only the profile, criteria, applicability, reviewed intervention, baseline gap, scenario, and boundary fields consumed by the UI; organization identifiers, write metadata, source custody, actor metadata, unused project/link records, and unused lifecycle/revision fields are omitted. Actual route attacks prove tenant-only workspace data, reject read-only creation/review/planning, reject cross-tenant source/property/profile/intervention references, preserve the foreign tenant unchanged, require distinct profile author/reviewer/publisher identities and independent intervention review, and prove minimized successful receipts plus explicit validation/conflict responses.
- Funding workspace, programme authoring/review/publication, eligibility, application, capital-stack, commitment/event, project-execution/workspace/assignment, milestone-event, payment-approval, and disbursement-export routes use the same request transaction as authentication. The organization and project presenters omit tenant/write/lifecycle metadata, credential digests, raw disbursement payloads, eligibility input facts/hashes, and unused internal fields. Route attacks prove tenant-only workspaces, read-only denial, cross-tenant source/project/programme/assignment denial, unchanged foreign-tenant state, distinct programme author/reviewer/publisher identities, separate commitment proposer/approver and payment approver/exporter, scoped external-project access, revocation, and minimized receipts. Project access tokens now use the same raw opaque-secret digest as tenant bootstrap, repairing previously unusable valid collaborator credentials.
- Verification workspace, verifier/credential/assignment, conflict, method, finding/review, exception/corrective-action, certificate/event, maintenance, and property-condition routes use the same request transaction as authentication. Organization and assignment presenters expose only UI-consumed governed fields and doctrine; tenant/write/lifecycle metadata, token digests, unused source fields, and internal confirmation flags are omitted. Actual handler attacks prove tenant-only reads, read-only denial, cross-tenant principal/credential/project/property denial, independent credential/finding/certificate decisions, exact evidence and corrective lineage, scoped external-verifier access, append-only certificate/maintenance history, and minimized receipts. Verification access tokens now use the same opaque-secret digest as request bootstrap, repairing previously unusable valid `fverify_` credentials.
- Integration workspace, connection create/status/health, schema registration, sync-job queue/run/replay, and webhook-endpoint registration use the same request transaction as authentication. The workspace presenter exposes only the provider, connection, schema, job, attempt, receipt, endpoint, delivery, health, and boundary fields consumed by the UI; tenant/write/lifecycle metadata, credential identifiers, raw configuration, idempotency/request hashes, payloads, storage/quarantine custody, leases, provider errors, and internal actors are omitted. Actual handler attacks prove tenant-only reads, read-only denial, cross-tenant credential/configuration/schema/job/connection denial, inline-secret rejection, idempotent queueing, fail-closed unavailable live providers, append-only dead-letter replay, tenant-scoped state transitions, unchanged foreign state, and minimized responses.
- Model-recognition workspace, organization/model/output/mapping/commitment creation, independent reviews, publications, and mapping events use the same request transaction as authentication. The workspace presenter exposes only UI-consumed provider/model/version/definition/output/mapping/evidence/event/organization/commitment fields and doctrine; tenant/write/lifecycle metadata, raw recorded values, property/evidence custody, internal authors, review notes, and unused mapping reviews are omitted. Actual handler attacks prove tenant-only reads, read-only denial, cross-tenant source/output/mapping/organization denial, distinct model and commitment author/reviewer/publisher authority, independent mapping review, explicit human-confirmed events, unchanged foreign state, and minimized responses.
- Recognition workspace, exact submission preparation/delivery, reviewer-session/workspace/revocation, clarification/response, separated market-response, case-closure, and maintenance-roll-forward routes use the same request transaction as authentication. Organization and reviewer presenters retain only UI-consumed submission, delivery, receipt, scoped session, correspondence, outcome, closure, maintenance, artifact, evidence, mapping, and doctrine fields; tenant/write/lifecycle metadata, token digests, external-principal/grant identifiers, internal actors, and storage custody are omitted. Actual handler attacks prove tenant-only reads, read-only denial, cross-tenant submission/delivery/closure denial, reviewer-principal isolation and revocation, human-confirmation gates, separated evidence/model/rating/underwriting/placement/funding outcomes, and minimized responses.
- Local development sign-in and OIDC start/callback run global attempt/rate-limit work under the non-owner application role and every invitation, membership, acceptance, and session operation under explicit tenant-local RLS context. Migration 0029 resolves only an organization identifier from an exact live invitation digest, one-use state digest, or provider-verified identity with exactly one active organization. OIDC network exchange occurs outside database transactions. Route and database attacks prove safe return-path normalization, missing-origin rejection before state consumption, one callback winner, replay rejection, cross-tenant session/acceptance denial, minimized responses, hashed state custody, and HttpOnly/SameSite cookies.
- Session read and logout use that same primitive; logout revocation and its audit append remain in the request transaction.
- The integration webhook uses a minimal `SECURITY DEFINER` bootstrap that returns only the organization identifier for an active endpoint. It then validates the HMAC and persists quarantine/job/audit records under that tenant's RLS context.
- The document worker requires explicit organization, actor, and worker identifiers and runs its dequeue/process mutation through `withTenantTransaction`.

## Security boundary

`fortify_resolve_request_tenant` accepts only an exact opaque token/state digest, credential prefix plus digest, endpoint key, or provider-verified identity subject plus provider key. It returns only an organization identifier, executes with a fixed search path, and is executable only by `fortify_app`. Identity-subject lookup is called only after the configured provider verifies the callback and returns a tenant only when exactly one active organization membership exists. After bootstrap, application code sets transaction-local tenant context and revalidates identity, membership, role, scope, assignment, expiry, revocation, and resource authorization before domain work. A bootstrap match is never authorization by itself. The inventory rejects an authenticated handler that returns the request promise without awaiting it, preserving each route's local authentication/error boundary.

## Remaining C0 work

Bind the remaining 11 authenticated programme-analytics routes, then make the inventory reject every remaining unbound entry point. Session rotation, the remaining cookie/CSRF/permission/response-minimization attacks, and managed PostgreSQL login proof also remain required. The production sandbox seeder is an explicit administrative fixture operation and is not counted as a request or worker entry point.
